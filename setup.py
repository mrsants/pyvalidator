from setuptools import setup, find_packages

setup(
    name="pyvalidator",
    version="0.1.0",
    description="validation for Python (Lambda-friendly)",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[],
    author="You",
    license="MIT",
    include_package_data=True,
)
